ADFSlib and ADF2INF

This is a snapshot of my ADFS floppy disc reading library and its associated
utility. The ADF2INF utility requires the cmdsyntax module.

Install it (as root) by typing:

  python setup.py install

